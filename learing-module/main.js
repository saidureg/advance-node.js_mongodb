// const { add } = require("./utils/add.js");
// const { subtract } = require("./utils/subtract.js");
const { add, subtract } = require("./utils"); // index.js is a special name in Node.js

console.log(add(30, 20)); // 50
console.log(subtract(30, 10)); // 20
